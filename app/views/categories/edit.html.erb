<h1 class="text-center mt-4 text-white">Edit category</h1>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
      <%= render 'edit_form' %>
    </div>

    <%= link_to '[ Cancel and return to categories listing ]', category_path(@category), class: "text-primary text-center" %>
  </div>
</div>
